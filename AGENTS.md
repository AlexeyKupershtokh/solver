# Описание проекта для LLM

Этот проект — **веб-приложение для пошагового решения математических уравнений** (преимущественно линейных), построенное на современном стеке React + TypeScript.

## 🛠 Технологический стек

- **Язык**: TypeScript (строгая типизация).
- **Фреймворк**: React 19.
- **Сборщик**: Vite.
- **Стилизация**: CSS.

## 📚 Ключевые библиотеки

1.  **`mathsteps`** (`^0.2.0`):
    - **Роль**: Ядро логики. Выполняет алгебраические преобразования.
    - **Функция**: Принимает строку с уравнением, возвращает массив шагов (`steps`). Каждый шаг содержит `changeType` (тип преобразования) и `newEquation` (новое состояние уравнения).
    - **Особенность**: Типы изменений возвращаются на английском (напр. `SIMPLIFY_ARITHMETIC`), требуя перевода для UI.
2.  **`react-mathquill`** (`^1.0.4`):
    - **Роль**: UI компонент ввода.
    - **Функция**: WYSIWYG редактор математических формул с поддержкой LaTeX.

## 📂 Архитектура и Файлы

- **`src/App.tsx`**: Корневой компонент. Синхронизирует состояние уравнения с URL-параметром `?eq=...`.
- **`src/components/MathInput.tsx`**: Обертка над `react-mathquill`. При изменении ввода вызывает `mathsteps.solveEquation(text)` и передает результаты в отображение.
- **`src/components/SolutionSteps.tsx`**: Рендерит список шагов решения.
- **`src/utils/mathstepsTranslations.ts`**: Словарь для локализации. Переводит ключи `changeType` из `mathsteps` на русский язык (напр., `COLLECT_LIKE_TERMS` -> "Собрать подобные члены").

## 🧠 Контекст для работы

- **Локализация**: При добавлении функционала проверяйте `src/utils/mathstepsTranslations.ts`. Если `mathsteps` вернет новый `changeType`, его нужно добавить туда.
- **Типизация**: Соблюдайте типы `MathStep` из библиотеки `mathsteps`.
- **Поток данных**: `MathInput` (ввод) -> `mathsteps` (расчет) -> `SolutionSteps` (вывод) -> `translateChangeType` (перевод).
